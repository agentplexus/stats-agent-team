{{- if .Values.synthesis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stats-agent-team.fullname" . }}-synthesis
  namespace: {{ include "stats-agent-team.namespace" . }}
  labels:
    {{- include "stats-agent-team.agentLabels" (dict "context" . "agent" "synthesis") | nindent 4 }}
spec:
  replicas: {{ .Values.synthesis.replicaCount }}
  selector:
    matchLabels:
      {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "synthesis") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "synthesis") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "stats-agent-team.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: synthesis
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "stats-agent-team.image" (dict "global" .Values.global "agent" .Values.synthesis) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.synthesis.service.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.synthesis.service.port | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "stats-agent-team.fullname" . }}-config
            {{- if .Values.secrets.create }}
            - secretRef:
                name: {{ include "stats-agent-team.fullname" . }}-secrets
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.synthesis.resources | nindent 12 }}
      {{- with .Values.synthesis.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.synthesis.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.synthesis.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
