{{- if .Values.direct.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "stats-agent-team.fullname" . }}-direct
  namespace: {{ include "stats-agent-team.namespace" . }}
  labels:
    {{- include "stats-agent-team.agentLabels" (dict "context" . "agent" "direct") | nindent 4 }}
spec:
  type: {{ .Values.direct.service.type }}
  ports:
    - port: {{ .Values.direct.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "direct") | nindent 4 }}
{{- end }}
