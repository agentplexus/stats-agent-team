{{- if .Values.orchestration.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stats-agent-team.fullname" . }}-orchestration
  namespace: {{ include "stats-agent-team.namespace" . }}
  labels:
    {{- include "stats-agent-team.agentLabels" (dict "context" . "agent" "orchestration") | nindent 4 }}
spec:
  replicas: {{ .Values.orchestration.replicaCount }}
  selector:
    matchLabels:
      {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "orchestration") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "orchestration") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "stats-agent-team.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: orchestration
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "stats-agent-team.image" (dict "global" .Values.global "agent" .Values.orchestration) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.orchestration.service.port }}
              protocol: TCP
            {{- if and .Values.orchestration.service.a2aPort (not .Values.orchestration.useEino) }}
            - name: a2a
              containerPort: {{ .Values.orchestration.service.a2aPort }}
              protocol: TCP
            {{- end }}
          env:
            - name: PORT
              value: {{ .Values.orchestration.service.port | quote }}
            {{- if and .Values.orchestration.service.a2aPort (not .Values.orchestration.useEino) }}
            - name: A2A_PORT
              value: {{ .Values.orchestration.service.a2aPort | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "stats-agent-team.fullname" . }}-config
            {{- if .Values.secrets.create }}
            - secretRef:
                name: {{ include "stats-agent-team.fullname" . }}-secrets
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.orchestration.resources | nindent 12 }}
      {{- with .Values.orchestration.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.orchestration.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.orchestration.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
