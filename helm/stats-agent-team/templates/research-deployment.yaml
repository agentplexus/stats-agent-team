{{- if .Values.research.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stats-agent-team.fullname" . }}-research
  namespace: {{ include "stats-agent-team.namespace" . }}
  labels:
    {{- include "stats-agent-team.agentLabels" (dict "context" . "agent" "research") | nindent 4 }}
spec:
  replicas: {{ .Values.research.replicaCount }}
  selector:
    matchLabels:
      {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "research") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "stats-agent-team.agentSelectorLabels" (dict "context" . "agent" "research") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "stats-agent-team.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: research
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "stats-agent-team.image" (dict "global" .Values.global "agent" .Values.research) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.research.service.port }}
              protocol: TCP
            {{- if .Values.research.service.a2aPort }}
            - name: a2a
              containerPort: {{ .Values.research.service.a2aPort }}
              protocol: TCP
            {{- end }}
          env:
            - name: PORT
              value: {{ .Values.research.service.port | quote }}
            {{- if .Values.research.service.a2aPort }}
            - name: A2A_PORT
              value: {{ .Values.research.service.a2aPort | quote }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "stats-agent-team.fullname" . }}-config
            {{- if .Values.secrets.create }}
            - secretRef:
                name: {{ include "stats-agent-team.fullname" . }}-secrets
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.research.resources | nindent 12 }}
      {{- with .Values.research.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.research.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.research.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
